# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
src/*.egg-info/
dist/*
!dist/*.whl
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Org-specific config (contains real table names, connection strings, etc.)
org_config.yaml

# Delta table outputs (local dev only; Fabric uses lakehouse paths)
data/output/

# Jupyter
.ipynb_checkpoints/

# Test artifacts
.pytest_cache/
htmlcov/
.coverage
data/output/
internal/presentation/

# Pre-anonymization source files (contain proprietary SQL/metadata)
data/synthetic/sepsis_sql/
data/Collibra*

# Local secrets (API keys for dev tooling) — never commit
.env
*.quarantine.json
llm_api_key.txt
.DS_Store
data/events/
internal/learning/
internal/private/

# Vendor-derived material — never leaves this machine
AIVIA_Protected/
AIVIA_Product/estates/*/governance/
AIVIA_Product/estates/*/.cache/

# THE WALL (Sunny's ruling 2026-09-18, Brief_Work_Dryrun):
# work estates exist only in the work clone and can never enter
# a commit — engine-outbound only, work data never comes back
AIVIA_Product/estates/work_*/

# Brief_Fabric_Resident FR1 (2026-09-19): build-time package copies
# (devtools/build_wheel.py) — the registries' one home stays
# AIVIA_Design/registries; these transients never enter a commit
aisql/_registries/
aisql/_libs/
aisql/_source_packs/
