{
  "_comment": "A15 BINDING (ruled 2026-09-05, landed at the land slice per the ruling's own schedule): per-target export column sets, bound from the term-propose precedent — native column sets as DATA, every rendered row checked against them (zero custom attributes, mechanically); NO provenance column anywhere — attribution rides the prefix inside the text. catalog_a is the engagement-neutral v1 target; real targets (Purview glossary CSV, Collibra Data Intake) bind per engagement from the same precedent shapes already proven in term_propose.",
  "attribution_prefix": "AIVIA agent generated: ",
  "targets": {
    "catalog_a": {
      "description": {
        "columns": ["Name", "Full Name", "Asset Type", "Domain", "Description", "Stewards"],
        "constants": {"Asset Type": "Logic Scope", "Domain": "AIVIA X-Ray"}
      }
    }
  }
}
