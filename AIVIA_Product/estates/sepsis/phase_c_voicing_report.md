# Phase C voicing census — the ED-sepsis corpus (Sunny's gap-check)

**Phase C of the twin-graph ruling (ADR 0077), built 2026-09-06,
carrying your four Phase B rulings (T-1 bless · T-2 operational
class · T-3 fix-for-demo · T-4 bless).** Floor grammar goes
**v2.0.0** — the policy-walk major. The full regenerated floors are
`gap_check_report.md` (303 scopes, generated by
`devtools/gap_check.py` — the generator is now a standing mechanism,
not an ad-hoc run). Your verdict on those floors is the phase gate.

## The finding-4 corpse, closed

`#Base_Pop_ED_Readmit` — the block that started the whole twin-graph
arc — now floors as:

> This is a selection of records.
> Drawn from the ed positivescores selection defined earlier in this
> procedure, restricted to records also present in the base pop
> selection defined earlier in this procedure, restricted to records
> also present in ed encounters dm records.
> - The date and time when the patient arrives at the emergency
>   department is between the ed departure time and 24 hours after
>   the ed departure time (inclusive).
> - The first time line is 1.

The readmit meaning is recoverable: a return ED arrival within 24
hours of departure, for records already in the base population. The
line-83 corpse (`dateadd(hh, 24, [#base_pop]...)` in prose) is dead —
RANGE bounds voice through the DATEADD phrase (ADR 0076's
evidence-ordered overlay; 12 estate uses ordered it) and columns-as-
values voice by steward words.

## What changed under your rulings

| Ruling | Landed as | Corpus effect |
|---|---|---|
| T-2 operational class | Registry v1.4.0 closed list; translated, voiced never | 275 statements out of debt; twin gaps 597 → 372 |
| T-3 fix-for-demo | 242 descriptions authored + 174 missing org-staging columns added to the demo catalog; runbook step 4c instructs customers | resolved 2,255 → 2,767; coverage gaps 3,208 → 1,483 |
| Gap B (queued at your T-3 review) | temp/CTE columns resolve through defining projections | 1,058 cross-scope resolutions |
| Unqualified-ref binding | single-source scopes bind safely; multi-source counted ambiguous | unresolved 9 → **27, all drift-class** (see below) |

**The drift surface GREW because visibility grew** — the 27 now
include vendor-table reads (ORGANISMS, GROUPER_MED_RECORDS columns
the dictionary never declared) that the old resolver never attempted.
Every one is a silently-failing-report finding. Kept counted forever.

**Build finds along the way** (each fixed at its design layer):
`IdentifierLiteral` (DATEADD's HH/DD tokens) was the ENTIRE
unmapped-expression class — 225 remainder rows erased (998 → 773);
the R1 lead no longer says "no source records are read" for scopes
reading earlier selections (it does now mean literally no sources).

## The voicing ledger, live

Corpus-wide: **427 decisions voiced + 203 counted == 630, disjoint.**
The counted side itemizes per scope: degenerate (policy-silent),
outer-join match conditions (voicing them as filters would lie),
unvoiced remainder. `produce.voicing_ledger(read, scope)` answers
"why isn't X mentioned" with a citable class — the error-contract
law, mechanized.

## Acceptance state

- F8 phase-C cases RUNNABLE and green
  (`tests/aivia/test_phase_c_voicing.py`): PC-1 composition +
  forbidden tokens, corpus-wide no-raw-bounds, PC-2 ledger balance,
  PC-4 depth cap, T-2-in-the-floors
- PC-3 standing corpses: the two-alias, R8-attribution, and
  inner/outer shakedown tests all green under the port
- F4 floor payload regenerated under grammar 2.0.0 (byte-exact
  tests green; the payload's ratification = your verdict here)
- **Your gap-check: `gap_check_report.md`** — the same pass you did
  for round 2, now over grammar 2.0.0. Two known rough edges to
  read with intent: (1) multi-table scopes with no joins captured
  voice every source as "combined with" (neutral, never false);
  (2) source phrases use readable table names ("ed encounters dm
  records") — table-description noun phrases are a polish item,
  not a truth item.
