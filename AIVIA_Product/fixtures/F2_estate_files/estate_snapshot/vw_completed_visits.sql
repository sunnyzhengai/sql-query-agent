SELECT E.ENCOUNTER_ID, P.PATIENT_NAME
FROM dbo.ENCOUNTER E
JOIN dbo.PATIENT P ON P.PATIENT_ID = E.PATIENT_ID
JOIN org_custom.ORG_READMIT O ON O.ENC_ID = E.ENCOUNTER_ID
WHERE 1 = 1
  AND E.APPT_STATUS_C IN (1, 2)
