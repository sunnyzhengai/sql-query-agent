{
  "What metrics are available?": "SELECT metric_id, metric_name, business_name, description\nFROM output_metric_logic ORDER BY business_name",
  "Tell me about a specific metric": "-- resolve the metric via semantic_search FIRST, then look up by exact id\nSELECT metric_id, metric_name, business_name, report_name, report_url, description, steward, developer, calculation_logic, source_tables, table_descriptions\nFROM output_metric_logic\nWHERE lower(metric_id) = lower('resolved_metric_id')",
  "Which metrics use a specific table?": "-- lineage: prefer readers_of_table('table_name') on the KQL source; this Lakehouse form is token-exact\nSELECT DISTINCT c.metricId, c.businessName\nFROM graph_edge_uses_table u\nJOIN graph_canonical c ON u.sourceId = c.nodeId\nWHERE lower(u.targetId) LIKE lower('%.table_name')",
  "How many metrics mention a term in their logic?": "-- token-bounded: 'ED' must match ED_SEPSIS, never COMPILED or other words merely containing the letters\nSELECT COUNT(*) AS metric_count\nFROM output_metric_logic\nWHERE lower(calculation_logic) LIKE '%[^a-z0-9]term[^a-z0-9]%'\n   OR lower(calculation_logic) LIKE 'term[^a-z0-9]%'\n   OR lower(calculation_logic) LIKE '%[^a-z0-9]term'",
  "Which metrics have no steward assigned?": "SELECT metric_id, metric_name, business_name FROM output_metric_logic WHERE steward IS NULL",
  "Show the recent build history": "SELECT TOP 20 * FROM ops_build_summary ORDER BY build_time DESC"
}
