# Shapes gap-check — ADR 0055 acceptance (designed shape corpus, diabetic-diagnosis palette)

Generated by devtools/shapes_gapcheck.py from a full LOCAL pipeline run (real ScriptDom parse → graph build → red-flag sweep) over data/shapes/generated/. The corpus is deterministic: regeneration is byte-identical (CI-asserted).

**Verdict: 26 instantiated cells, 26 pass, 0 fail; 5 excluded-with-reason; every ratified dimension value covered.**

## Corpus totals

- 22 SQL files, 21 parsed; declared parse exceptions: [('reporting.USP_Adhoc_Extract', 'no_query')]
- graph: 140 nodes, 294 edges; projection 103 minted (6 via the temp chase), drops {'duplicate': 11, 'step_projection_ambiguous': 3, 'step_projection_untracked': 2, 'ambiguous': 1}
- sweep: 15 flags over 63 swept items (37 flagged, 25 clean, excluded {'no_fragment': 1}) — conservation asserted

## Dimension coverage (totality — the 0052 pattern)

- **D1_name_relation**: identical → S1/S2/S3/S4/S5/S6/M1/M3/M4/M6/M7, cousin → S8/M2/M3, disjoint → S6/S7/M4/M5
- **D2_logic_relation**: hash_identical → S1/S6/M3/M4/M7, ws_case_only → S2/M6, sem_same_syn_diff → S3/M8, genuinely_different → S4/S5/S7/M1/M2/M5
- **D3_scope**: cte → S1/S2/S3/S4/S6/S7, temp → S5/R3, schema_view → S9, business → M1/M2/M3/M4/M5/M6/M7
- **D4_reference_form**: direct_qualified → R1, aliased → R2, via_temp_projection → R3, unqualified_unique → R4, unqualified_ambiguous → R5, wrong_kind → R6
- **D5_chain_shape**: linear → C1, diamond → C2, self_reference → C3, cross_schema_twin → C4
- **D6_hygiene**: dynamic_sql → H1, multi_statement → H2, crlf → H3, phi_literal → H4

## Cell matrix — expected vs actual

| cell | dims | status | result |
|---|---|---|---|
| S1 | step.cte.identical.hash_identical | instantiated | PASS — expected = actual |
| S2 | step.cte.identical.ws_case_only | instantiated | PASS — expected = actual |
| S3 | step.cte.identical.sem_same_syn_diff | instantiated | PASS — expected = actual |
| S4 | step.cte.identical.genuinely_different | instantiated | PASS — expected = actual |
| S5 | step.temp.identical.genuinely_different | instantiated | PASS — expected = actual |
| S6 | step.cte.disjoint.hash_identical | instantiated | PASS — expected = actual |
| S7 | step.cte.disjoint.genuinely_different | instantiated | PASS — expected = actual |
| S8 | step.*.cousin.* | excluded | EXCLUDED — sweep v1 computes cousin families at metric grain only (ADR 0054 §3b inventory); step-grain cousins are a recorded follow-up |
| S9 | step.schema_view.* | excluded | EXCLUDED — schema-object sweep is a recorded ADR 0054 follow-up; v1 artifact classes are steps and metrics |
| M1 | metric.business.identical.genuinely_different | instantiated | PASS — expected = actual |
| M2 | metric.business.cousin.genuinely_different | instantiated | PASS — expected = actual |
| M3 | metric.business.cousin.hash_identical | instantiated | PASS — expected = actual |
| M4 | metric.business.disjoint.hash_identical | instantiated | PASS — expected = actual |
| M5 | metric.business.disjoint.genuinely_different | instantiated | PASS — expected = actual |
| M6 | metric.business.identical.ws_case_only | instantiated | PASS — expected = actual |
| M7 | metric.business.identical.hash_identical | excluded | EXCLUDED — the M6 pair already proves the identical-name normalized-equal control; a byte-identical twin adds no coverage |
| M8 | metric.*.sem_same_syn_diff | excluded | EXCLUDED — the D2 boundary is a property of the normalizer, proven at S3; the metric hash composes the same normalized keys |
| R1 | ref.direct_qualified | instantiated | PASS — expected = actual |
| R2 | ref.aliased | instantiated | PASS — expected = actual |
| R3 | ref.via_temp_projection | instantiated | PASS — expected = actual |
| R4 | ref.unqualified_unique | instantiated | PASS — expected = actual |
| R5 | ref.unqualified_ambiguous | instantiated | PASS — expected = actual |
| R6 | ref.wrong_kind | excluded | EXCLUDED — ask-surface cell (lineage(table=<metric name>) → the W9 redirect stamp) — no SQL file exists for it; covered by the walk section and the live leg, recorded here for totality |
| C1 | chain.linear | instantiated | PASS — expected = actual |
| C2 | chain.diamond | instantiated | PASS — expected = actual |
| C3 | chain.self_reference | instantiated | PASS — expected = actual |
| C4 | chain.cross_schema_twin | instantiated | PASS — expected = actual |
| H1 | hygiene.dynamic_sql | instantiated | PASS — expected = actual |
| H2 | hygiene.multi_statement | instantiated | PASS — expected = actual |
| H3 | hygiene.crlf | instantiated | PASS — expected = actual |
| H4 | hygiene.phi_literal | instantiated | PASS — expected = actual |

## What each instantiated cell planted (for the gap-check read)

- **S1** (step.cte.identical.hash_identical): clean control: Lab_Draws — files: reporting/USP_Diabetic_Patients_DX.sql, reporting/USP_Diabetic_Patients_Lab.sql
- **S2** (step.cte.identical.ws_case_only): clean control: Enc_Recent — files: reporting/USP_Diabetic_Patients_DX.sql, reporting/USP_Diabetic_Patients_Lab.sql
- **S3** (step.cte.identical.sem_same_syn_diff): flag misnomer/INFO on A1c_High (2 logic(s)); DISCLOSED boundary: differs by normalized hash — the verdict never claims semantic difference (D2 boundary) — files: reporting/USP_Diabetic_Patients_DX.sql, reporting/USP_Diabetic_Patients_Lab.sql
- **S4** (step.cte.identical.genuinely_different): flag misnomer/INFO on Base_Cohort (2 logic(s)) — files: reporting/USP_Diabetic_Patients_DX.sql, reporting/USP_Diabetic_Patients_Lab.sql
- **S5** (step.temp.identical.genuinely_different): flag misnomer/INFO on Dx_Events (2 logic(s)) — files: reporting/USP_Ref_Forms_Probe.sql, reporting/USP_Panel_Refresh.sql
- **S6** (step.cte.disjoint.hash_identical): flag duplicate/INFO on unnamed duplicate (1 logic(s)) — files: reporting/USP_Diabetic_Patients_DX.sql, reporting/USP_Diabetic_Patients_Lab.sql
- **S7** (step.cte.disjoint.genuinely_different): clean control: Reg_Core, Reg_Core_v1 — files: reporting/USP_Diabetes_Registry.sql, reporting/USP_Diabetes_Registry_v1.sql
- **M1** (metric.business.identical.genuinely_different): flag misnomer/CONFLICT on Active Diabetic Patients (2 logic(s)); compare verdict DIFFERS — files: reporting/USP_Active_Diabetics.sql, reports/USP_Active_Diabetics.sql
- **M2** (metric.business.cousin.genuinely_different): flag cousin_conflict/CONFLICT on Diabetes Registry (2 logic(s)) — files: reporting/USP_Diabetes_Registry.sql, reporting/USP_Diabetes_Registry_v1.sql
- **M3** (metric.business.cousin.hash_identical): flag cousin_conflict/INFO on Controlled Diabetes Rate (1 logic(s)) — files: reporting/USP_Controlled_Diabetes.sql, reporting/USP_Controlled_Diabetes_M.sql
- **M4** (metric.business.disjoint.hash_identical): flag duplicate/INFO on unnamed duplicate (1 logic(s)); compare verdict IDENTICAL — files: reporting/USP_Diabetic_Roster.sql, reporting/USP_DM_Patient_List.sql
- **M5** (metric.business.disjoint.genuinely_different): clean control: A1c Testing Compliance — files: reporting/USP_A1c_Compliance.sql, reporting/USP_Diabetes_Registry.sql
- **M6** (metric.business.identical.ws_case_only): clean control: Enrollment Snapshot — files: reporting/USP_Enrollment_Snapshot.sql, reports/USP_Enrollment_Snapshot.sql
- **R1** (ref.direct_qualified): 1 projection edge(s) — files: reporting/USP_Ref_Forms_Probe.sql
- **R2** (ref.aliased): 1 projection edge(s) — files: reporting/USP_Ref_Forms_Probe.sql
- **R3** (ref.via_temp_projection): 1 projection edge(s) — files: reporting/USP_Ref_Forms_Probe.sql
- **R4** (ref.unqualified_unique): 1 projection edge(s) — files: reporting/USP_Ref_Forms_Probe.sql
- **R5** (ref.unqualified_ambiguous): drop buckets {'ambiguous': 1}; the BARE PATIENT_ID ref drops as ambiguous (two candidate tables); the QUALIFIED join refs in the same CTE legitimately mint — the drop bucket, not edge absence, is the assertion — files: reporting/USP_Ref_Forms_Probe.sql
- **C1** (chain.linear): 2 chain edge(s) — files: reporting/USP_Care_Cascade.sql
- **C2** (chain.diamond): 4 chain edge(s) — files: reporting/USP_Outreach_Funnel.sql
- **C3** (chain.self_reference): recursive CTE — build must complete with no dangling edges (cycle broken at the back-edge) — files: reporting/USP_Panel_Rollup.sql
- **C4** (chain.cross_schema_twin): structural expectations — files: reporting/USP_Active_Diabetics.sql, reports/USP_Active_Diabetics.sql
- **H1** (hygiene.dynamic_sql): dynamic SQL — the declared exception path (suppressed extraction or zero extractable steps), never a crash — files: reporting/USP_Adhoc_Extract.sql
- **H2** (hygiene.multi_statement): structural expectations — files: reporting/USP_Panel_Refresh.sql
- **H3** (hygiene.crlf): clean control: Crlf_Cohort — files: reporting/USP_Crlf_Probe_A.sql, reporting/USP_Crlf_Probe_B.sql
- **H4** (hygiene.phi_literal): structural expectations — files: reporting/USP_Phi_Probe.sql

## DEMO NOTE — the 3 most demoable cells

1. **M1 — the Active Diabetic Patients conflict** (same business name, two schemas, genuinely different logic — the classic silent disagreement).
   Ask: *"Are the two Active Diabetic Patients definitions the same?"*
   Expect on screen: compare partition with **2 hash groups — logic DIFFERS** (W15 stamp), plus the metric misnomer CONFLICT flag naming both members; caption must echo "differs" (gate-enforced).
2. **M2 — the Diabetes Registry (Legacy v1) cousin** (the drift beat, estate-wide).
   Ask: *"What governance red flags exist for the Diabetes Registry?"*
   Expect: the cousin_conflict CONFLICT flag row — 2 members, 2 distinct logics, no official designated; "how is Diabetes Registry defined" stamps certified-variants-exist.
3. **S4 — the Base_Cohort misnomer** (same CTE name in two procs, materially different populations — the Base_Pop story on a plantable corpus).
   Ask: *"Are all definitions of Base_Cohort the same?"*
   Expect: the step-name universe stamp ("2 procs have a step NAMED 'Base_Cohort' — a name match is not logic sameness") carrying the recorded misnomer flag (INFO, 2 logics); compare shows the dx-path vs lab-path diff.

Each is falsifiable on camera: the sins were PLANTED with their oracles, so "it found exactly these" is a checkable claim, not a vibe. (Demo-switch mechanism + tenant load: PARKED for Sunny — see the handoff RESULTS.)
