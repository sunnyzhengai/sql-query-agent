# Review — Demo recording readiness (2026-08-23)

**Verdict: the CONFIDENCE is earned; the CAPTURE is not ready today.**
Nothing capability-shaped blocks recording — what stands between here
and capture day is a bounded list: one product decision (Sunny's),
one script-refresh pass, two in-flight items to land first, and the
walk's remaining steps as the final acceptance. No missing
deliverable class was found.

## Earned (the confidence case)

- **Honesty machinery is demo-true**: 1.00 on every family across
  consecutive runs, both tiers; the gates caught real inventions live
  (walk, Round-4 prep). VO-1's central claim — "answers proven by
  code, not by confidence" — is now mechanically enforced on screen.
- **Round 4 closed**, goal complete as amended; Marketplace claim set
  approved (13/13 vs 8/13, faster p50, reach, consistency,
  lineage-by-parse).
- **Pipeline is turn-key in fact**: full chain 030→800 ran unattended
  via the job API, 45 min, zero failures; C4 grounding 0-missing;
  conservation + reachability audits clean. VO-2's claims hold.
- **Corpus is clean**: anonymization escape fixed and CI-armed; 28
  synthetic metrics; Screening Trend URL ruled accepted.
- **Production apparatus exists**: canonical DEMO_SCRIPT.md (V1 flow,
  TTS rules, capture checklist), QA gate, current INSTALLATION_GUIDE
  (Step 6 = agent config), listing/security/reviewer docs,
  architecture diagram.

## Blockers (sequence before capture day)

1. **Script/product drift** — DEMO_SCRIPT.md and AGENT_DEMO_GATE.md
   are 2026-08-16 vintage: the QA-gate transcripts show the RETIRED
   surface (probe-eh, search_catalog→get_facts Basis format), and
   tenant-prep cites env v1.11.0 (now 1.56.2) and manual chain steps
   the job-API run supersedes. Every on-screen chat beat must be
   re-verified verbatim on the CURRENT surface and the QA gate
   re-run; the gate file's answers re-captured.
2. **THE SURFACE DECISION — RULED (Sunny, 2026-08-23): WORKBENCH
   ONLY; no Fabric agent in the demo.** All chat beats run on the
   workbench (typed verdict, stamped headlines, live-streamed ops
   trail). The SQL Intelligence Agent remains a customer-buildable
   artifact (INSTALLATION_GUIDE Step 6) but stays off camera. Script
   refresh is unblocked.
3. **Land the in-flight work first**: the lineage-mitigation republish
   (Fabric agent is changing this week) and the retirements (Graph
   Agent, eh_probe, notebook moves) — capture against the final,
   clean tenant.
4. **Walk steps 2–6** (~30 min, Sunny live): memory, pointer chase,
   honest wall, misname, surprise round. This IS the "enough
   confidence?" instrument — L3 acceptance by her own protocol. Any
   failure becomes a fixture before its fix ships, per standing law.
5. **Mechanics**: capacity resumed; webapp restarted on the current
   wheel; fresh conversation; description field verified empty.

## Risks to manage (not blockers)

- **Drilldown at 0.33**: the script's headline question is
  definition-family (strong) — keep capture improvisation away from
  "how is a patient diagnosed"-shaped asks, or budget retakes. The
  failure mode is humble/refusal (on-brand), never fabrication.
- **2026-08-16 credential exposure — STATUS (Sunny, 2026-08-23):**
  Collibra is work-controlled, not Sunny's to rotate; it has been
  REMOVED entirely from AIVIA config (the work/AIVIA wall holds —
  exposure remediation belongs to the work side). Purview app secret
  rotation DEFERRED — open item on Sunny, do before capture day.
- **VERIFY: VO-6 "Available now on the Microsoft Marketplace"** —
  claims audit against the real listing status before the final cut.
- **Column lineage on the 7 new tables** is absent (no dict_columns
  rows yet — refs drop as no_dictionary_column). If a column-lineage
  beat is added, use COMPILED_CONTEXT (verified, 5 filters). Optional
  backfill: dictionary rows for the 7, one rerun.

## Missed-deliverables scan: none found, three open threads

- **M2 drilldown capability** (HANDOFF_M2_DECISION_EVIDENCE) — open
  design work, post-demo.
- **B3 dep-chains** (transform_to_transform) — was PARKED BEHIND
  ROUND 4; Round 4 is closed, so this now awaits Sunny's
  green-light or explicit re-park.
- **Script upgrade opportunity** (Sunny's call, V1 flow is her
  ruling): the new differentiators earned since 08-16 — typed
  honesty verdict on screen, exact readers-of-table lineage — fit
  naturally as one line in VO-4's blast-radius beat; the refusal
  beat VO-3 already has.
