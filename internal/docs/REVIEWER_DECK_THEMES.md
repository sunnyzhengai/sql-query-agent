# Microsoft reviewer deck — six themes (recorded 2026-08-25; Sunny thinking)

Discipline: every claim on every slide is backed by a repo artifact
— the deck surfaces what we keep; we never claim for the deck.

1. **The line + category.** "AIVIA turns the code your organization
   already trusts into answers everyone can trust." One diagram:
   SQL estate in → governed answers out, inside the customer's
   Fabric tenant. Ten-second repeatability test.
2. **The problem, verbatim.** Unreadable proc estates; "no one can
   find which reports to trust"; "will things break if I change
   this table?"; one name, six definitions. (Sources: Bill, Fang,
   hospital BI reality.)
3. **Architecture: Fabric-native, consumption-driving.** Native
   parsers → knowledge graph in OneLake/Eventhouse/Lakehouse →
   agent surfaces. BYOT, Entra ID, customer's Azure OpenAI. The
   co-sell slide: every customer is Fabric capacity + OneLake +
   AOAI tokens — AIVIA sells Microsoft.
4. **Proof, not promises.** (a) 13/13 vs 8/13 oracle-scored on
   identical questions (approved claim set wording only);
   (b) honesty machinery — evidence-or-named-refusal, one live
   gate-catch screenshot; (c) the planted-sins shape corpus with
   receipts. The falsifiability slide.
5. **Better together (the delicate slide).** AIVIA FEEDS the stack:
   Purview + PBI write-back, grounds the Fabric Data Agent, gives
   tenant AI an ontology extracted from code. Frame: the governance
   layer Microsoft's AI ecosystem consumes. NEVER phrasable as a
   Copilot competitor.
6. **Business + trust + ask.** "Basic governs the definitions; Pro
   runs them"; the flywheel in one sentence; security posture (BYOT
   no data liability, passthrough identity, rows never enter model
   context). Ask: certification + co-sell. Diligence artifacts
   already in docs/product/ (whitepaper, architecture PDF, reviewer
   guide).
