# Brief_M7_Consumption_Governance — the report's derived pair by cherry-pick; governance serves; the phase boundary runs whole

**Status: CLOSED** (Sunny's "done", 2026-09-19: the dev prefix re-upload landed — graph_file + graph_pbi_report, ONE refresh, Q3 GREEN on the final form. The full arc: RE-SCOPED 2026-09-18 to THE CONSUMPTION LAYER ONLY (governance → M8, design-first); LOAD 1 GREEN same day (Q1 6169 · Q2 6690 · Q3 the AI-generated pair · Q4 67); LOAD 2 (the phase-boundary sepsis reload) DEFERRED by his word — sepsis exports stand regenerated and ready for whenever he calls it.)

## THE RE-SCOPE (Sunny's ruling, 2026-09-18 — supersedes Q2's serve-at-M7)

His words: *"can we update the design doc to make M7 only the
consumption layer? i don't see why we need to work governance
into this layer. and make governance M8. plus we need to really
design M8 before implementing."* And his design positions for
M8, recorded the same sitting: *"in my opinion, descriptions
should be properties. … i agree with you, actors should be
nodes. records in general should not be nodes."*

What this changes:

- **M7 = consumption only**: pbi_report node · executes edge ·
  the derived pair (technical_definition by cherry-pick + the
  approved Scribe summary, both as report PROPERTIES) ·
  bound_fields · the phase-boundary sepsis reload (consumption +
  the prior layers only).
- **Governance → M8**: blessed_name · description · agent · role
  · responsibility (+ the may-be-0 journal citizens) do NOT ride
  the M7 load. Q2's "yes to fabric" stands as intent but its
  landing batch is M8, AFTER the M8 design sitting rules the
  node-vs-property question per item (his positions above are
  the design inputs; the Contract_Governance_Layer holds them).
- **The trigger finding stands recorded** (FG3 in
  Contract_Governance_Layer): the governance serve shipped node
  tables with almost no edge tables — the store holds those
  connections as properties, connect.py derives them locally,
  the export never projected them. M8's design resolves this
  properly instead of patching it.

Unwind build steps (ALL LANDED 2026-09-18, his "yes, run the
unwind build"; the ratified order kept):

1. Gate sheet + answer key re-based to consumption-only M7
   (LANDED with this amendment — same breath; the key's basis
   consciously bumped to 1.49.0 with its bump-record entry).
2. Registries 1.49.0 via the converter (SERVED_LABELS re-based;
   Design_Graph_Engine stamp block moved same breath — RG-A2
   green) → the failing export pin authored FIRST
   (test_governance_stays_home_until_m8, red then green) →
   export_graph.py: the governance tables + graph_describes code
   OUT (pbi_report + executes stay). LANDED.
3. Both estates' exports regenerated — the governance parquets
   (graph_blessed_name · graph_description · graph_agent ·
   graph_role · graph_responsibility) DELETED, both estates at
   the same 38-table consumption set; ALL 85 prior parquets
   verified byte-identical by sha256 before/after. LANDED.
4. TEST_MAP regenerated (195 modules / 2114 tests) · ruff: zero
   new offenses (the two pre-existing convert_from_xlsx E501s
   stand) · FULL SUITE 2219 passed / 0 failed (12:25,
   2026-09-18). LANDED.

Files-declared amendment (his word above = the H5 word): +
AIVIA_Design/Design_Graph_Engine.md · + AIVIA_Design/INDEX.md.

## THE AI-GENERATED PREFIX (Sunny's ruling at the served gate, 2026-09-18)

His words, at LOAD 1's Q3: *"in the pbi_report description, can
we always add this phrase 'AI-generated: ' to the beginning of
the description"* — scope ruled at the presented question: **"All
agent-authored"** (the report + the 28 sepsis file descriptions;
one mechanical rule, no special case).

The design: the ONE landing writer (receive_descriptions'
node-landing step, the M6 one-writer) derives the prefix from
agent authorship — node property = `"AI-generated: "` + the
approved artifact text. The approved texts in descriptions.json
and on the description artifacts stay EXACTLY as approved (his
approval act untouched); the prefix is provenance made visible,
derived at landing. The idempotency compare moves to the
prefixed form. Pins re-based: the report byte pin
(test_report_layer) · the artifact-vs-property relation pin
(estate battery) · the gate sheet's Q3/M6 served texts (dated).
Served data changes → the dev re-upload (graph_file +
graph_pbi_report) + ONE refresh, his hand; sepsis regens BEFORE
LOAD 2 (no wasted load).

| field | content |
|---|---|
| class | planned addition — fills the ladder's M7 slot (consumption + governance; the phase boundary) |
| claims | the M7 answer-key rows (2026-09-10) AS AMENDED by the 2026-09-17 design sitting: **Q1 "a"** — the bindings come FIRST (the SemanticModel's imported columns → the snapshot; the TMDL is IN the repo, tables/*.tmdl carry column/sourceColumn) · **Q2 "yes to fabric"** — GOVERNANCE SERVES: the 2026-09-13 "never in the Fabric export" ruling is SUPERSEDED (recorded in the ruled row); blessed_name 113 · agent · role · responsibility · description artifacts ride to Fabric with pbi_report + executes; the old M7 gate numbers (6286/6691) stand as SERVED numbers · **Q3 "a"** — multi-proc composition CONCATENATES, labeled by proc, one caged summary over the whole (dormant today: 1 resolves, 1 counted-unresolved) · **Q4 "a"** — model-grain cherry-pick + THE DISCLOSURE LINE ("filters shown are the procedure's; the report may filter further in Power BI"); THE REPORT-LAYER EXTRACTION (visual-grain bindings + page/visual filters + DAX transforms from the .Report folders) = a NAMED future brief, its ledger row landing with this batch · **Q5** — contracts-first: Contract_Consumption_Layer.md + Contract_Governance_Layer.md BORN WITH the batch (closing CI-B5) · **Q6 "run all and scribe drafts all 27. it's ok to pay for this"** — the full sepsis estate re-runs whole; the Scribe drafts ALL remaining file descriptions (his spend authorized); the full-sepsis Fabric reload rides the close (his hand) |
| impacts (computed) | **the cherry-pick chain**: a new extractor reads the SemanticModel tables → `bound_fields` per report lands in pbi_snapshot/reports.json (both estates; the sepsis estate's reports.json is AUTHORED here too — the ruled 2026-09-08 mapping: every proc feeds a PBI, shells synthetic, mapping real) → the report node gains its TWO FIELDS: technical_definition = the executed files' catch-alls CHERRY-PICKED to bound fields + population whole + the disclosure line, concatenated per Q3 when plural; description = the Scribe's caged summary of that. **executes becomes a real edge** (1 resolved + counted-unresolved list); describes edge serves. **EXPORT_LABELS extends** (Q2): pbi_report · description · agent · role · responsibility · blessed_name (+ person/term/usage as may-be-0) with their tables + edges. **The Scribe batch**: 27 new file descriptions (evidence = each file's R13 catch-all; authorized spend, well under a dollar) → THE CAGE run mechanically per sentence → presented as ONE BATCH for Sunny's approval (the blessing-batch precedent) → approved texts land per estate descriptions.json. **The recording**: ~28 new index sentences → AIVIA_RECORD run (his authorized spend). **Registries 1.48.0** via the converter (EXPORT_LABELS mirror · Connection_Ledger pbi/executes rows to served form · the Q2 supersession stamped). **Two contract docs born.** **Served data changes → TWO loads at close, his hand: the dev-estate M7 delta + THE FULL SEPSIS RELOAD (one refresh each — the boundary act he authorized).** Gate sheet: §M7 queries authored WITH the sheet; the key's `pbi_description_derived_via_executes` check re-based to the cherry-pick form |
| ambiguities | M7-A · M7-B below — the rest resolve at the TWO CHECKPOINTS by design: (1) the report's rendered pair presented before ratifying the report-grain rider; (2) the 27 drafts presented as a batch with cage results before any lands |
| debt declared | THE REPORT-LAYER EXTRACTION (Q4): visual-grain bindings + PBI-side filters + DAX — a named future brief; until it lands, every report technical definition carries the DISCLOSURE LINE (visible, never silent). The unresolved executes entries stay a COUNTED list |
| Sunny's approval | **"approved"** — 2026-09-17, M7-A's proposal accepted with the word; M7-B resolves at checkpoint 1 by design |
| closing check | BALANCED at CLOSED (2026-09-19): every changed file sits in the declared list as amended (the re-scope amendment added Design_Graph_Engine.md + INDEX.md at his word; the prefix rider's pins in already-declared suites; registries/exports = the generated exempt classes). Gate catches this batch: the test-file omission in Brief_Work_Dryrun's list caught at build start (declared before any write) — zero undeclared edits landed. Served: dev gate Q1–Q4 GREEN on the final form, his hand, three refreshes total across the arc (LOAD 1 + the cleanup + the prefix re-upload). LOAD 2 deferred = an OPEN ledger item, not debt: the sepsis exports are regenerated and byte-stable |

## The ambiguities — Sunny rules each

| id | question | proposal (Sunny may overrule) |
|---|---|---|
| M7-A | the `bound_fields` snapshot form | a per-report list of column names, exactly as the SemanticModel's table definitions import them (`sourceColumn`), auto-date tables excluded (LocalDateTable_* is PBI plumbing, not proc output); the extractor is deterministic and re-runnable (devtools, free, my run) |
| M7-B | the report-grain rendering (an R13 rider, gap-checked at checkpoint 1): section forms for the cherry-picked Presents · the per-proc label wording ("From USP_ED_SEPSIS:") · the disclosure line's exact words · FLOOR_GRAMMAR_VERSION 2.12.0→2.13.0 at ratification with the two conscious pins | authored DRAFT suite-first; THE REAL RENDERED PAIR for the ED Sepsis dashboard presented; Sunny ratifies |

## Files declared

    aivia/flows/inbound.py
    aivia/flows/produce.py
    aivia/flows/describe.py
    aivia/flows/export_graph.py
    aivia/flows/connect.py
    AIVIA_Design/registries/convert_from_xlsx.py
    AIVIA_Design/Grammar_Floor.md
    AIVIA_Design/Contract_Consumption_Layer.md
    AIVIA_Design/Contract_Governance_Layer.md
    AIVIA_Design/Contract_Logic_Layer.md
    AIVIA_Design/Contract_Surfaces.md
    AIVIA_Product/estates/ed_sepsis_dev/expected_m_gates.json
    AIVIA_Product/estates/ed_sepsis_dev/pbi_snapshot/reports.json
    AIVIA_Product/estates/ed_sepsis_dev/descriptions.json
    AIVIA_Product/estates/sepsis/pbi_snapshot/reports.json
    AIVIA_Product/estates/sepsis/descriptions.json
    AIVIA_Product/estates/sepsis/expected_shakedown.json
    AIVIA_Test/test_ed_sepsis_dev_estate.py
    AIVIA_Test/test_speech_parity.py
    AIVIA_Test/test_meaning_console.py
    AIVIA_Test/GQL_Gates.md
    tests/aivia/test_report_layer.py
    tests/aivia/test_file_render.py
    tests/aivia/test_graph_export.py
    tests/aivia/test_shape_census.py
    tests/aivia/test_connection_census.py
    tests/aivia/test_metamodel.py
    tests/aivia/test_visual_counts.py
    tests/aivia/test_part_edges.py
    tests/aivia/test_derived_render.py
    tests/aivia/test_produce.py
    tests/aivia/test_center_censuses.py
    devtools/pbi_extract.py
    devtools/graph_visual/generate_m1.py
    devtools/graph_visual/m1_template.html
    docs/architecture/TEST_MAP.md
    AIVIA_Design/briefs/Brief_M7_Consumption_Governance.md
    AIVIA_Design/Design_Graph_Engine.md
    AIVIA_Design/INDEX.md

(Enumerated generously — the M5/M6 lesson, now with the census
and parity suites included up front since served-label growth
touches them. Amending after approval needs Sunny's word, H5.)

## Build order (after APPROVED)

1. devtools/pbi_extract.py: SemanticModel → bound_fields; BOTH
   estates' reports.json regenerate (sepsis's authored: shells
   per proc + the real dashboard, the 2026-09-08 mapping).
2. Registries 1.48.0 (EXPORT_LABELS mirror · ledger rows · the
   Q2 supersession) + the two contract docs BORN (DRAFT).
3. Tests FIRST, failing: the report-pair pins · executes-edge
   pins · served-governance export census · the M7 battery
   evolutions · the sepsis re-run battery hooks.
4. The report layer builds: executes edges · the cherry-pick
   composer (R13 rider DRAFT) · CHECKPOINT 1: the ED Sepsis
   dashboard's rendered PAIR to Sunny → ratify → 2.13.0.
5. THE SCRIBE BATCH (authorized): 27 drafts from the catch-alls
   → mechanical cage per sentence → CHECKPOINT 2: the batch to
   Sunny → his approval → texts land → the AIVIA_RECORD run
   (authorized).
6. Full suite + ruff · exports regen BOTH estates · contracts
   populated → his gap-check → RATIFIED.
7. Sunny's loads: the dev M7 delta + THE FULL SEPSIS RELOAD
   (one refresh each) · §M7 gates served · the republish →
   CLOSED.
