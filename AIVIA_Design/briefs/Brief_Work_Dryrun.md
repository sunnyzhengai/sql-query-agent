# Brief_Work_Dryrun — work SQL through the engine, descriptions seen; the wall holds

**Status: BUILT** (2026-09-18, same sitting as the approval: the wall guard + pilots scaffold + the driver landed, refusal pins red-then-green, FULL SUITE 2221/0 (12:05), ruff zero new offenses; CLOSED follows Sunny's work-side dry run — findings return as rows)

| field | content |
|---|---|
| class | planned addition — the first step of the work pilot (Sunny's wall ruling 2026-09-18: "wall opens for engine-outbound only, work data never comes back"; work permission for personal code confirmed by him same day) |
| claims | Sunny's words: "i would like to test work sql files using our codebase to-date, and see the descriptions." Scope = SQL descriptions ONLY — the Collibra publishers and PBI intake configuration are the NEXT briefs (his gap #5/#3); this brief gets work SQL booting and speaking |
| impacts (computed) | NO engine code changes — the estate boot already accepts any estate folder with the right scaffold. NEW: pilots/work_dryrun/ (runbook + templates, placeholders only — zero work specifics ever in this repo) · .gitignore gains the work-estate guard (`AIVIA_Product/estates/work_*/`) so a work estate in his work clone CANNOT be committed even by accident · devtools/scribe_draft.py (a small driver for the existing describe.scan_undescribed→evidence→draft→land chain, so his work run is one command, AIVIA_RECORD-style: runs only at his hand with his key) |
| ambiguities | W1 · W2 below |
| debt declared | the ALLOWLIST SHIPMENT MANIFEST (his "never mix" rule made mechanical) is Brief_Packaging's work, not this brief's — until it lands, the guard is the gitignore pattern + the wall rule; counted here so it cannot be forgotten |
| Sunny's approval | (pending) |
| closing check | (filled at CLOSED) |

## The ambiguities — Sunny rules each

| id | question | proposal (Sunny may overrule) |
|---|---|---|
| W1 | the dictionary at work: the estate boot expects a KG1 dictionary extract (`<source>_snapshot/` with columns.csv + joins.csv from the source-pack SQL). Full extract against work's Clarity = real column words in every description. SQL-only boot (empty dictionary) = everything resolves to counted drift, renders thin | run the FULL source-pack extract at work (his work is Epic Clarity; the pack + SOP runbook exist and ship by intent); the SQL-only fallback stays documented as plan B |
| W2 | which descriptions the dry run shows: deterministic only (technical definitions + scope/statement renders — land at boot, free, immediate) vs + Scribe file summaries (his WORK OpenAI key, the cage, his approval batch) | BOTH — deterministic at boot, then the Scribe driver on the small batch (pennies, his hand); approved texts land with the AI-generated prefix exactly as here |

## The wall, enforced in this brief

- Engine-outbound only: he clones the repo AT WORK; the work estate
  is born in the WORK clone as `AIVIA_Product/estates/work_<name>/`.
- The `work_*` gitignore pattern means that estate can never enter
  a commit, even by accident, in either clone.
- pilots/work_dryrun/ in THIS repo holds placeholders only — no
  work paths, no work names, no work SQL, no outputs, ever.

## Files declared

    AIVIA_Design/briefs/Brief_Work_Dryrun.md
    .gitignore
    pilots/work_dryrun/README_Runbook.md
    pilots/work_dryrun/registration_template.json
    devtools/scribe_draft.py
    tests/aivia/test_scribe_draft.py
    docs/architecture/TEST_MAP.md
    AIVIA_Design/INDEX.md

(tests/aivia/test_scribe_draft.py added at build start — the
build-order's "test-first" step named its file; the omission
was the author's, caught before any write; his "approved"
covers the tests-first plan.)

## Build order (after APPROVED)

1. .gitignore guard + pilots/work_dryrun/ born (runbook: the
   numbered work-side steps — clone, python3.11 + DOTNET_ROOT +
   the DLL check, source-pack extract, estate scaffold, boot,
   console browse; registration template with placeholders).
2. devtools/scribe_draft.py: test-first (a pin that it refuses
   without an estate arg + a key; drafting itself is his run).
3. Full suite + ruff.
4. Sunny runs the dry run AT WORK; what he learns lands back
   here as FINDINGS ROWS ONLY (words about the engine, never
   work data) → feeds Brief_Packaging + Brief_Collibra.
