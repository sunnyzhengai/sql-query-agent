# Brief_Pilot_Findings_R1 — the work dry run's ten findings become the build plan; the target sentence is the Business Term definition

**Status: APPROVED** (DRAFT → PRESENTED → APPROVED → BUILT → CLOSED) — all ten ambiguities ruled in one sitting, 2026-09-19; per ruling (7) this brief is the findings-and-rulings RECORD: the build lands via three ship-unit briefs that cite it (1: A+A2+B · 2: C · 3: D+E), each carrying its own files-declared list and closing check

| field | content |
|---|---|
| class | mixed, by slice: A + B are fixes (code returns to the error-contract law and the turn-key census); C is updates to ruled voicing (Grammar_Floor amendments ride his rulings); D is an unplanned addition (a new composition for scope/CTE descriptions) — D blocks on rulings, A can build on approval |
| claims | F6–F9 (Contract_Technical_Layer) · FL9–FL18 (Contract_Logic_Layer) — landed 2026-09-19 from the first AND second work dry-run samples, voiced as abstract shapes only (the wall: no work identifiers in this repo; every finding reproduces on the sepsis corpus, F9 via a db-qualified fixture) |
| impacts (computed, step-2 query) | A: kg1_intake.load_snapshot + every intake text read (utf-8-sig; named refusals INTAKE-family for malformed JSON / missing headers) — consumers: dry_run, connect, the runbooks' "what good looks like" lines. B: pilots runbook + SOP (estate manifest step) or fabric_run writes it (his call). C: condition/join/derived-column renderers + Grammar_Floor rules (scope naming · function phrases · _C suffix · join fallback · LAG/LEAD · CASE double-walk investigation at the generator per the echo law). D: the scope/CTE description writer recomposed grain + population + payload; consumers: every stored scope description (VERBATIM LAW: stored == recomputed → the sepsis estate re-voices, HIS LOAD if served data changes), the coming Collibra Business Term export (Brief_Collibra, queued). Tests first for every slice; new spoken sentences may hit RecordingGap → his AISQL_RECORD run |
| ambiguities (HIS RULINGS — code blocked until ruled) | (1) RULED (Sunny "i agree with your recommendation", 2026-09-19): grain speaks AUTOMATICALLY from definitional sources only — GROUP BY and DISTINCT; window-partition grain speaks at the scope that APPLIES the rank filter (the scope holding "rank = 1"), never at the scope that merely computes the ranking (the second dry-run sample proved the computing scope still holds every row); no grain source → the sentence opens WITHOUT the grain clause, never an invented one; NO steward gate — under this rule nothing spoken is inference, every grain traces to a defining structure. (2) RULED (Sunny "i agree", 2026-09-19): population = inner joins spoken as MEMBERSHIP + value conditions from inner-join ONs + EVERY WHERE condition regardless of source table (WHERE always drops rows — a WHERE filter on a left-joined column silently converts the lookup into a restriction, proven live in the second sample, and the sentence must not claim a wider population than the query returns); EXCLUDED: pure join-key equalities (membership already says them — the FL9 noise) and outer-join ON predicates (they decide what the lookup matches, never who is in). (3) RULED (Sunny "i agree with the sentence form", 2026-09-19): TWO shapes — with grain "One record per <grain>: <population restrictions>, carrying <payload>."; grain absent → "<base source> records: <population restrictions>, carrying <payload>."; population items join with "; "; the word is "carrying"; LONG value lists compress to a count in the sentence ("not one of N excluded plan codes") — the full list stays on the condition rows; R-numbered into Grammar_Floor at build; replaces the from-structure lead FOR SCOPES ONLY. (4) RULED (Sunny "i agree, new ids", 2026-09-19): NEW ids — and the numbering corrects: INTAKE-11/12/13 already exist (content hash · equivalence audit · unchanged-hash versioning), so the new rows are INTAKE-14 (a file exists but its content does not parse — named with path + parse error) and INTAKE-15 (CSV headers mismatch the contract — named with expected vs found, per file); INTAKE-0 stays untouched ("a part is missing"); rationale ruled in: distinct remedies get distinct ids (the error-contract law) and per-id firing counts across pilots are product signal (the error-contract philosophy); BOM itself stops being an error — utf-8-sig at every intake read. (5) RULED (Sunny "i agree with option c", 2026-09-19): the wheel writes a manifest TEMPLATE and intake enforces the one human field — extract_scripts() writes estate_snapshot/manifest.json with location (from the folder) and default_schema (from the source pack — a vendor fact) filled and as_of EMPTY; intake refuses by name on empty as_of ("fill in the date this estate SQL was captured") — the refusal is the template's tripwire per the placeholder law, so it never ships naked; the runbook step shrinks to typing one date; both refusals (missing manifest · empty as_of) get NEW ids at build per ruling (4)'s pattern. (6) RULED (Sunny "i agree with option a", 2026-09-19): per-vendor naming knowledge lives IN THE SOURCE PACK — the clarity pack gains a naming-conventions table (suffix → voicing rule: _C drops the suffix and expects a category values map; _YN speaks as a yes/no flag; the exact list authored at build from Epic's documented conventions), pack version bumps to clarity-pack-1.2; the voicing ladder: blessed name → pack convention → readable-identifier fallback; the grammar floor stays vendor-free (an Epic fact is not an English fact); estates with no pack are untouched; FL11's values-map half is not ambiguous and rides the build under the ruled R8 precedence. (7) RULED (Sunny "i agree, three briefs", 2026-09-19): split BY SHIP-UNIT at approval — brief 1: A+A2+B (fixes, no stored-text change, one wheel release; A2 first-and-fast, it unblocks honest re-testing of the work estate) · brief 2: C (voicing — stored descriptions change, one Grammar_Floor bump, one regen, ONE load) · brief 3: D+E (the sentence redesign + the over-capture it stands on, its own ratification and load); Brief_Pilot_Findings_R1 closes as the findings-and-rulings RECORD — the three build briefs cite it as their claim and carry their own files-declared lists and closing checks. FROM THE SECOND SAMPLE: (8) RULED (Sunny "i agree with option b", 2026-09-19): the db part stays meaningful, COUNTED-NEVER-REFUSED — registered db == ref's db → binds (the match IS the cross-check passing); registered db != ref's db → the ref stays unresolved, counted as a cross-database read WITH THE DB NAMED (legitimate SQL, and the count is product signal toward multi-source registration); db_name omitted (the minimal path) → schema-only binding stands per the waiver, but the census REPORTS distinct db names seen so a risky waiver becomes visible; the INTAKE-8/9 REFUSAL stays on the dictionary/extract side only — estate SQL never refuses on a db name. (9) RULED (Sunny "i agree, fold entirely", 2026-09-19): FOLD ENTIRELY — one NOT predicate, one condition row, the folded voice; the child row is never minted; the folded row ABSORBS the child's column-resolution links (verified: today the child carries them — _condition_refs stops at child predicates); the tree shape survives in the parse record; a future ask for the un-negated form DERIVES it at ask time, never stores it; condition census re-bases and the re-base must reach EVERY coverage string (the FL7 lesson); lands in build-brief 2 (slice C). (10) RULED (Sunny "i agree with option a", 2026-09-19): the composer reads the TREE at compose time — a member with a dcol node speaks its ruled phrase; a renamed pass-through speaks the author's alias words (the author-named voicing line, no blessing gate); a plain pass-through speaks the column's spoken name; NO new nodes — the sealed M4 census stands, verbatim holds because the recompute walks the same tree (the scope lead's existing pattern); payload walkability stays with FL1 (the computed-read edge), whose own queued brief owns that question. |
| debt declared | none new; FL13 is investigation-first (generator clause), no patch before the double-walk is located |
| retirement (pivots only, the 2026-09-19 law) | none — D replaces the from-structure sentence FOR SCOPES only; the from-structure family stays for tables/files |
| does this promote? (the Promotion Gate) | at close, at his word, after his load if served text changes |
| Sunny's approval | APPROVED 2026-09-19 — all ten ambiguities ruled in conversation, each quoted in the ambiguities row above |
| closing check | files changed == files declared (filled at CLOSED) |

## The slices, in build order

- **A — intake hardening (fix)**: every intake file read decodes
  utf-8-sig; malformed JSON and missing/wrong CSV headers refuse by
  name (file path, expected vs found), per the error-contract law.
  Tests first: BOM'd manifest fixture, BOM'd CSV fixture, headerless
  CSV fixture — each expecting its named refusal. (F6, F7)
- **B — the estate manifest step (fix)**: the pilots runbook gains
  the step; the missing manifest becomes a named refusal; ambiguity
  (5) decides whether the wheel writes it. (F8)
- **C — voicing fixes (updates, post-ruling)**: scope naming in
  condition prose (FL9 — the defect-masking find is the acceptance
  test's shape) · function phrases (FL10) · _C suffix (FL11) · join
  fallback names both sides (FL12) · LAG/LEAD phrases (FL14) ·
  the CASE double-walk located at the generator before any patch
  (FL13). Each lands as a Grammar_Floor rule + renderer change +
  sepsis-corpus pins, RED first.
- **A2 — three-part name resolution (fix, second sample)**: db-qualified
  names (`db.schema.table`) split db · schema · table and bind on
  schema — today the schema lookup key is `db.schema` and can never
  hit, so a whole proc's tables land unresolved and every downstream
  voice degrades (F9's cascade: FL12's fallback, bare name words, no
  declared value meanings). Test first: a db-qualified sepsis fixture,
  RED on resolution census. Ambiguity (8) decides the db part's
  further use.
- **C (adds from the second sample)**: FL16 the NOT-child double voice
  (post-ruling, ambiguity 9) · FL18 "Both of its parts hold" · FL10
  gains its two verified entry points (the condition path never calls
  the fill library; DATEADD's guard misses negative offsets).
- **D — the Business Term sentence (unplanned addition)**: scope/CTE
  descriptions composed as grain + population + payload from nodes
  the graph already holds. The pilot's target consumer: Collibra
  Business Terms. Blocks entirely on ambiguities (1)–(3).
  CORRECTED (second sample): grain is NOT graph-held today and
  payload only partly — D depends on slice E (the over-capture) and
  on ambiguity (10)'s payload-source ruling.
- **E — the over-capture (echo-mandated, second sample)**: the
  mapper's over=True flag becomes captured PARTITION BY / ORDER BY
  contents; the recorded deferral's echo arrived (FL17), so per the
  Echo Law this builds — sequencing Sunny's. Acceptance test: a
  partitioned ROW_NUMBER dcol whose stored phrase names its
  partition and its ordering.

## Files declared

Per ruling (7), this brief is the record, not the build — its own
files are the record's files; the three build briefs declare their
build files themselves:

    AIVIA_Design/briefs/Brief_Pilot_Findings_R1.md
    AIVIA_Design/Contract_Technical_Layer.md
    AIVIA_Design/Contract_Logic_Layer.md
