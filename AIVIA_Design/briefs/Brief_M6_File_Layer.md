# Brief_M6_File_Layer — the file joins the served graph with THE TWO GOVERNANCE FIELDS; the containment chain completes; the 31-statement debt retires

**Status: CLOSED** (2026-09-17 — the second batch of the one continuous sitting to run its whole arc inside the process and under the gate: design sitting → four rulings + the R13 checkpoint ("all three", "ratified") → his Scribe run → the cage walk → "APPROVED" → his recording run → suite 2212/0 → his load → Q1–Q5 served GREEN ("Q1 to Q5 all passed") → the republish → this close. His gate-verdict message ALSO caught the stale composition row — the reader of last resort working)

| field | content |
|---|---|
| class | planned addition — fills the ladder's M6 slot ("M6 holds the next entry gate", the M5 closing stamp) |
| claims | ds.birth_edge_law (file→statement retires the 31 counted-missing — the Connection_Ledger row's named landing step) · the containment law (file→statement→scope→condition, Design_Graph_Engine) · THE SPEECH CONTRACT 2026-09-09 (a file's speech is its OWN aboutness, Scribe-drafted, gated drafted→approved; undescribed = COUNTED gap never a wall) · the English ladder (LLM compression at the top grain, gated — re-homed CI-B1 territory, cited by section) · the hash-chain law (approvals anchor to contentKeys) · the Shape_Ledger file row ("TARGET stored — today artifact-linked; landing: the materialization build" — M6 IS that landing) |
| impacts (computed, step-2 query over the contracts + the key) | **new graph data** (answer key M6): file ×1 joins the SERVED graph · has_part +71 (file→statement 67 — closing the debt · file→param 4) · census after: 6168 nodes / has_part 6166 / edge total 6689. **writers**: `_store_file_layer` (or extension of the file intake) in aivia/flows/inbound.py — edges + the stored description landing; the approve flow (aivia/flows/describe.py) if M6-1 rules (a). **registries**: 1.46.0→1.47.0 via the converter — Shape_Ledger file row TARGET-obligation flips stored; Connection_Ledger statement row's counted-missing clause RETIRES (edged via has_part); the has_part edge row's "TARGET remains: file→statement at M6" clause closes. **consumers**: sc.ask_console — the file ALREADY speaks (aboutness artifact since 2026-09-09; searchability does NOT change; if the stored text == the approved artifact text, NO new sentences, NO recording run) · sc.meaning_console walks the new edges (battery) · sc.fabric_export ships graph_file.parquet + graph_has_part_fileStatement + graph_has_part_fileParam (NEW; the file's tree/twin blobs NEVER ride — M6-4) · sc.graph_visual: file joins the FS1 counting labels (test compares census_after.M6). **served data changes → Sunny's load, ONE refresh.** **known pin evolutions, declared UP FRONT (the M5 lesson — five late amendments)**: test_metamodel version pin 1.47.0 · test_connection_census counted_missing_kinds ["statement"]→[] (the debt CLOSES — the pin celebrates it) · test_shape_census (file row obligation) · test_speech_parity (file verifier per M6-1) · test_visual_counts/NODE_LABELS gain file · estate battery M6 store tests + ladder freezes |
| ambiguities | M6-1 RULED (the design sitting); M6-2 · M6-3 · M6-4 · M6-5 · M6-6 below — open or defaulted; each blocks its slice |
| debt declared | none new; this batch RETIRES the 31-statement counted-missing debt (declared at M5, landing step honored) |
| Sunny's approval | **"yes, yes, yes. approved"**; R13 checkpoint: "all three" (C1 prune degenerates · C2 recurse · C3 group by selection) then **"ratified. yes"** (the catch-all text RATIFIED + connect.py declared — the gate's M6 catch: adjacency never learned the file→statement spine); "yes" again: the description-status closed set gains APPROVED (the vocabulary the speech contract promised, first reached tonight) + kg3_artifacts.py declared for the mirror; "yes. commit": TEST_MAP.md declared (the map regen for the two new test files — the M5 ride) + the commit word, held for the closing suite — 2026-09-17: M6-3 the walk re-base · M6-4 the row shape · M6-6 not-index-speech, then the build word; M6-1 ruled at the design sitting; M6-2/M6-5 resolve at the R13 checkpoint by design |
| closing check | **BALANCED** — the declared files == the changed set across the build commit (c769c13) + the closing stamps, plus the exempt classes (registry JSONs regenerated · design-doc same-breath stamps · the 3 new export parquets declared in impacts, all 33 prior byte-identical). THE GATE'S M6 LEDGER: three catches (connect.py · kg3_artifacts.py · TEST_MAP.md), each declared at Sunny's word; zero undeclared edits landed. Served verdict verbatim: "Q1 to Q5 all passed" |

## The ambiguities — Sunny rules each

| id | question | proposal (Sunny may overrule) |
|---|---|---|
| M6-1 | **the ONE writer of the stored file.description** — and, at Sunny's pause ("i want to pause and take a moment to design the right solution"), the whole two-field design | **RULED (Sunny, 2026-09-17, the design sitting — his words: "confirmed" ×3):** THE TWO GOVERNANCE FIELDS, Collibra-named, each ONE writer: **(1) technical definition = R13 THE CATCH-ALL** — a deterministic render, verbatim-law, two halves: THE PRESENTED COLUMNS (the delivery's output list; computed outputs speak their R12 phrases) + THE POPULATION LOGIC (his decision #1: "all population defining conditions, join (inner) and conditions" — every WHERE condition on the delivery chain + the INNER joins with their ON conditions; outer joins excluded — they do not restrict the population). **(2) report description = the Scribe's caged summary OF the technical definition** (his chain: "we summarize this technical definition using LLM" — the catch-all subsumes what-is-presented AND who-qualifies, so it is the ONE evidence; the old anatomy/shell evidence retires), drafted → HIS approval, meaning-key anchored, empty-until-approved COUNTED. **Both computed at the FILE ("file level is catch-all"), derived at the PBI report at M7 by CHERRY-PICKING** ("PBI report level is cherry picking from the file level"): the report's definition = the file's catch-all filtered to its bound fields + the population logic whole; its description = the Scribe summary of the cherry-pick — report-specific by construction. Collibra first, Purview later (his decision #2); files stay lineage in Collibra, reports are the customer-facing assets. CONSEQUENCE, declared: the existing 2026-09-09 draft ("sepsis cases, their severity classification…") REDRAFTS from the new basis (evidence = the R13 render; new prompt/basis stamp) — Sunny approves the redraft; the Scribe call is a PAID run, HIS key, his hand; the changed index words need his AIVIA_RECORD run (declared, never silent) |
| M6-2 | the cage check's mechanical form for the description | the cage compares the summary against **the R13 technical definition** (the new one evidence): no concept in the sentence absent from the catch-all — sharper than the old anatomy comparison; the summing direction stays rejected (it would resurrect the wall). Sunny rules the wording at the checkpoint |
| M6-3 | the walk gate's stale strings (the FL7 class, found at this brief's authoring): the key's walk says `scope-[:left_side\|right_side\|reads]->table` and GQL_Gates' M6 row says `scope-[:reads]->table` — but the reads STORE edge RETIRED at era 3; the ruled walk is `file-[:has_part*]->scope—has_part→(join \| direct_read)—sides→table` | **RULED (Sunny "yes" #1, 2026-09-17)** — re-base both strings to the era-3 walk in this build (the Brief_Gate_Number_Rebase precedent) |
| M6-4 | the graph_file.parquet row shape — the file node's local properties include the TREE BLOB (huge, internal) and the twin linkage | **RULED (Sunny "yes" #2, 2026-09-17)** — hand-built row: nodeId · name · description · **technicalDefinition** · contentHash · loadedAt ONLY; blobs NEVER ride; the meaning-key anchor stays a LOCAL integrity fact |
| M6-5 | §R13 THE CATCH-ALL's exact form — section order (columns first or population first) · condition selection mechanics (the delivery chain's WHERE set; degenerates; duplicates) · the inner-join line shape ("combined with X on A = B"?) · list idioms | authored DRAFT suite-first, pins failing, **THE REAL RENDERED CATCH-ALL for USP_ED_SEPSIS presented at a checkpoint** (the 36-sentences arc), Sunny ratifies → FLOOR_GRAMMAR_VERSION 2.11.0→2.12.0, the two version pins move consciously |
| M6-6 | is the technical definition ask-index SPEECH? | **RULED (Sunny "yes" #3, 2026-09-17)** — NO: a node speaks ONE property; the file's speech stays the approved description; the technical definition displays in the meaning console and exports for Collibra |

## Files declared

    aivia/flows/inbound.py
    aivia/flows/describe.py
    aivia/flows/export_graph.py
    AIVIA_Design/registries/convert_from_xlsx.py
    AIVIA_Product/estates/ed_sepsis_dev/expected_m_gates.json
    AIVIA_Test/test_ed_sepsis_dev_estate.py
    AIVIA_Test/test_speech_parity.py
    AIVIA_Test/test_meaning_console.py
    AIVIA_Test/GQL_Gates.md
    tests/aivia/test_file_layer.py
    tests/aivia/test_graph_export.py
    tests/aivia/test_shape_census.py
    tests/aivia/test_connection_census.py
    tests/aivia/test_metamodel.py
    tests/aivia/test_visual_counts.py
    tests/aivia/test_speech_contract.py
    tests/aivia/test_file_render.py
    tests/aivia/test_derived_render.py
    tests/aivia/test_produce.py
    aivia/flows/produce.py
    aivia/flows/connect.py
    aivia/graph/kg3_artifacts.py
    docs/architecture/TEST_MAP.md
    AIVIA_Design/Grammar_Floor.md
    AIVIA_Product/estates/ed_sepsis_dev/descriptions.json
    devtools/graph_visual/generate_m1.py
    devtools/graph_visual/m1_template.html
    AIVIA_Design/briefs/Brief_M6_File_Layer.md

(Enumerated generously up front — the M5 lesson: five mid-build
amendments; a declared file that goes untouched costs nothing and
the closing diff reports it. Amending after approval needs
Sunny's word, per H5.)

## Build order (after APPROVED)

1. Registry rows via the converter (1.47.0): the Shape_Ledger
   file flip (BOTH fields in the obligation) · the
   Connection_Ledger debt retirement · the has_part clause close.
0. (threads through) §R13 THE CATCH-ALL authored DRAFT in
   Grammar_Floor → render pins FAILING → the builder renders the
   REAL catch-all → CHECKPOINT: Sunny reads it → ratify →
   2.12.0. THEN the Scribe redraft from the new basis (HIS paid
   run + his approval + his AIVIA_RECORD run for the changed
   index words).
2. Tests FIRST, authored failing: the M6 estate battery (file
   served, +71 edges, debt-closed check, the cage check, the
   era-3 walk) · test_file_layer structural pins (F2 fixture,
   twin-recomputed) · export pins (graph_file shape — blobs
   absent) · the declared pin evolutions.
3. Code: the edges + the description landing (per M6-1) + the
   export tables + the FS1 label.
4. Full suite + ruff · export regen (new: graph_file + the two
   has_part tables; all else byte-identical).
5. Sunny's load + ONE refresh · the §M6 gates (queries authored
   WITH the sheet this time — the M5 lesson) · the visual
   republish (FS1 green first) · close with the declared-vs-
   actual diff; the Connection_Ledger retirement stamps same
   breath.
