# Brief_Dryrun_Order — dry_run speaks the composed texts first: files, scopes, statements before the dictionary flood

**Status: BUILT** (DRAFT → PRESENTED → APPROVED → BUILT → CLOSED;
BUILT 2026-09-20 — suite 792/0 (5:40), ruff zero new, closing
check BALANCED 12==12; CLOSED at his next dry_run's eyes) —
Sunny's directive IS the ruling and the approval
(2026-09-20, from his work pilot: "the dry run output a lot of
texts, but i can't find the scope descriptions or statement or
sql file's. can you update to show these descriptions first?").

| field | content |
|---|---|
| class | update — the FR5 surface (fabric_run.dry_run, Brief_Fabric_Resident "speak the node census and every governance text") changes its SPEAKING ORDER; no stored field, no writer, no graph change |
| claims | FR5 (the dry_run vocabulary) amended by his quote above |
| impacts (step-2 query) | fabric_run.dry_run only — a read-only renderer; NO stored data changes, no load, no export, verbatim law untouched (the texts printed are the same bytes, reordered). Consumers: his notebook cell output. Tests: test_fabric_run gains the order pin (RED first); the existing census/both-kinds pin must stay green |
| ambiguities | none — the order follows his words: file · scope · statement first (the composed descriptions), then every remaining label grouped with a header and count so the dictionary flood is skippable, total line unchanged |
| debt declared | none |
| retirement (pivots only) | none |
| does this promote? | rides the standing queue |
| Sunny's approval | the directive quoted above (2026-09-20) |
| closing check | BALANCED (2026-09-20): 12 files changed == 12 declared (the 2.3.0 wheel line = the ruled deletion); suite 792 passed / 0 failed (5:40) in one clean pass; ruff zero new; wheel 2.3.1 sha256 f8517085… |

The order: census line first (unchanged) → `=== file (n) ===` →
`=== scope (n) ===` → `=== statement (n) ===` → the remaining
labels alphabetically, each under its own header with its count,
identities sorted inside each group → the total line (unchanged).

THE WHEEL 2.3.1 rides this brief (one-current-wheel law — the
change exists FOR his work notebook, so it ships now; 2.3.0
retires; the runbook URL follows).

## Files declared

    AIVIA_Design/briefs/Brief_Dryrun_Order.md
    aisql/fabric_run.py
    tests/aisql/test_fabric_run.py
    tests/aisql/test_wheel_boot.py
    pyproject.toml
    CHANGELOG.md
    dist/sql_query_agent-2.3.1-py3-none-any.whl
    dist/sql_query_agent-2.3.0-py3-none-any.whl
    pilots/work_dryrun/README_Runbook.md
    AIVIA_Design/INDEX.md
    AIVIA_Design/Manifest_Build.md
    docs/architecture/TEST_MAP.md
