# Audit_Contract_Integrity — the first integrity review of the contract system against the prose design

**Status: EXECUTED 2026-09-17 (three parallel sweeps: forward ds-resolution · reverse coverage · cross-doc consistency). THE MECHANICAL PASS LANDED same day at Sunny's "continue when done": CI-A1–A6, C1–C3 built (re-homed laws marked for his gap-check); C4 + all 17 B rows remain OPEN on him.** Run at Sunny's "can we review its integrity? can we rewrite it as contract not prose" — the evidence here informs that ruling.

**Sunny's gap-check queue from the mechanical pass: ALL FIVE PASSED (Sunny, 2026-09-17, one at a time — "pass" ×5):** the SCOPE IDENTITY section · the description-obligations sentence · THE BATCH LAW paragraph (all Design_Graph_Engine) · THE RULED-SILENT LIST rider (Design_Chatbot) · the one-invariant COVERAGE amendment (Design_Graph_Engine). Every pending marker cleared in place; CI-A1–A6 + C1–C3 fully CLOSED.

## THE VERDICT, one paragraph

The contract system's rows are sound where they point at real design — but of 15 distinct ds ids, SIX have no state-doc home (two live only in the ledger, which our own law says is history never current law; three point at the ledger although a real home exists; one points at a generated registry sheet whose own ruling row defers to a document that no longer exists), one more is only one-third homed, and SEVENTEEN ruled prose sections govern data or surfaces with no contract row at all — including an entire un-contracted layer (L0/L1 evidence) that FL3's twin retirement already depends on. One live-text contradiction was found (scopes: ruled-silent by a ledger line, in-the-index by the live design). The prose docs are NOT redundant — they are load-bearing and in places the ONLY home of law; the failure mode is missing homes, missing rows, and missing anchors, not prose itself.

## A — FORWARD: ds ids that do not resolve cleanly

| id | finding | kind | proposed disposition | status |
|---|---|---|---|---|
| CI-A1 | ds.scope_identity's home "kg2_logic Scope_Identity" is a GENERATED registry sheet (kg2_logic.json:191) whose own _ruling row says "Doc is authority; transcribed verbatim-in-substance" — but that doc (the pre-rename design document with models A3/A4/A11) exists only in git history. A generated data file cannot be a ds home: the converter rerun rewrites it. The sheet also rules a clause the contract drops (plural emitters mint delivery_1..n AND surface on the DBA list) | broken authority chain | re-home the identity law as a ruled section in Design_Graph_Engine (transcribed from the sheet, Sunny gap-checks); the sheet's _ruling row then points at the new home | LANDED 2026-09-17 (SCOPE IDENTITY section, gap-check pending; the sheet's pointer update rides the next registry release) |
| CI-A2 | ds.m1_batch is LEDGER-ONLY (Manifest_Build:185); its load-bearing clause — db/db_schema descriptions none-ruled, zero is legal — was never written as ruled law anywhere, yet is now cited as precedent by the 2026-09-17 statement ruling (b). A never-written rule is doing governing work twice removed | ledger-only law | land the none-ruled clause as a ruled sentence in Design_Graph_Engine's L2 section; ds row re-points | LANDED 2026-09-17 (gap-check pending) |
| CI-A3 | ds.birth_edge_law — the widest-scoped ds id (governs EVERY dc row's shipping batch) — lives only in the ledger (Manifest_Build:193-196) plus an EXECUTED audit (Audit_Graph_Integrity_Plan:39). No Design_/Ruling_ doc carries it | ledger-only law | land the batch law in Design_Graph_Engine (the migration/ladder section is its natural home) | LANDED 2026-09-17 (THE BATCH LAW, gap-check pending) |
| CI-A4 | three ds ids point at the ledger although a real state-doc home EXISTS: ds.m3_condition_layer → Design_Graph_Engine:176-179 · ds.m4_stay_flat → :294-296 · ds.cites_store_grain → :296-297. Also: the cites grain's tuple definition "distinct (scope, dictionary column)" is contract-only text — neither doc states it | misrouted pointers | fix the three home-doc cells; land the tuple definition in the Design_Graph_Engine cites sentence | LANDED 2026-09-17 |
| CI-A5 | ds.ruled_silent_index is one-third homed AND hides a live CONTRADICTION: the contract row names joins + direct_reads + statements; Design_Chatbot rules only joins (:1061); direct_read appears NOWHERE in Design_Chatbot; statements are ruled only by a ledger line (Manifest_Build:203-205) which ALSO rules scopes silent — contradicting the live design (Design_Chatbot:1058 "Scopes join the index: all 44") and the contract's own consumer row (scope feeds sc.ask_console). Reality (the built index): scopes IN, statements/joins/direct_reads OUT | conflict row — Sunny decides | rule the full silent list in Design_Chatbot (the riders section): joins · direct_reads · statements silent, scopes IN; the ledger line stands as history of the pre-§D state | LANDED 2026-09-17 (THE RULED-SILENT LIST rider, gap-check pending — the contradiction dissolves under the state-vs-ledger split: the ledger line is history) |
| CI-A6 | small drifts: the "schema"→db_schema scar's rationale is contract-only (Design_Graph_Engine never says db_schema exists BECAUSE schema is reserved) · R8's one-liner drops the COUNTED-disagreement clause · R10 is cited by GOVERNS + CONTRACT_FIELDS but missing from the DESIGN_SECTIONS row · era-3 walk notation differs between contract and doc (same law) | wording drift | four one-line doc/contract touch-ups, one act | LANDED 2026-09-17 (scar sentence · R8 COUNTED clause · R10 row · cites notation) |

## B — REVERSE: ruled prose with no contract row (17 gaps, three tiers)

| id | finding | should land in | status |
|---|---|---|---|
| CI-B1 | THE ENGLISH LADDER (Design_Graph_Engine:192-208) — the writer-selection law behind EVERY description field (vendor verbatim → grammar render → LLM compression gated drafted→approved; "a compressed description may only contain concepts present in its anatomy") has no ds id; dc.file.description's "LLM cage" cites nothing | ds.english_ladder in Contract_Logic_Layer, governing every description row + the sc.grammar_render/Scribe boundary | OPEN — tier 1 |
| CI-B2 | THE L0/L1 EVIDENCE LAYER (Design_Graph_Engine:40-101) — append-only, nothing-read-without-storing, parse_records per (contentHash, parserVersion), english_drafts status ladder, derivation_runs conservation, structural-facts-get-no-L1-store — an ENTIRE un-contracted layer; FL3's twin retirement "at M9 (parse records)" points at a table no contract holds | a fourth layer doc (Contract_Evidence_Layer) — the biggest structural hole | OPEN — tier 1 |
| CI-B3 | THE HASH CHAIN + REBUILD LAW (:98-101, :210-213) — contentKey + provenance columns are RULED onto every node row (:137-140) and appear in NO CONTRACT_FIELDS table; approvals anchor to contentKeys (survive unchanged meanings, visibly orphan changed ones) — the declared drift-detection mechanism, un-rowed | ds.hash_chain + contentKey/provenance field rows in BOTH layers | OPEN — tier 1 |
| CI-B4 | THE ONE-VOCABULARY + TOTAL-SCORE LAWS (Design_Chatbot:338-373) — acronym nodes with approved_by/used_by edges DERIVED AT GRAPH BUILD (a node label + two edge families with a declared writer = graph data) have no dc row, no ds id, no consumer row | dc.acronym + edges in the governance layer; total-score into Surfaces GOVERNS | OPEN — tier 1 |
| CI-B5 | the consumption + governance node/edge vocabularies (pbi_report·executes; description·term·usage·disposition·proposal·acronym·person·agent·role + describes/performed_by/approved_by/used_by/supersedes/assigns — Design_Graph_Engine:110-135, :142-176) are un-rowed; Contract_Consumption_Layer + Contract_Governance_Layer are promised in Technical's header and do not exist | the two missing layer docs | OPEN — tier 1 |
| CI-B6 | THE MEANING-CONSOLE RIDERS (Design_Chatbot:1001-1318: matched graph, pass-through, structure-never-rows, near-first, meaning-card default, mechanical net, ownership default, member grain) — all RULED, all governing sc.meaning_console, whose promises cell says only "speaks only stored text; walks only store edges" | Surfaces GOVERNS, several ds ids (separately amendable) | OPEN — tier 2 |
| CI-B7 | THE LIVE-WIRE TOGGLE (Design_Chatbot:1128-1163) — a live Fabric-calling surface with a capacity law, counted spends, and a security rule (the console NEVER stores a token) — in NO sc row | sc.live_wire in SURFACE_CONTRACTS | OPEN — tier 2 |
| CI-B8 | THE TURN DEFAULT's operative clause ("a surface that omits the choice point needs a ruling naming why") binds every kind=surface row; no sc row cites it | Surfaces GOVERNS, own row | OPEN — tier 2 |
| CI-B9 | THE SEATS (Design_Chatbot:204-216 rights table + the namer seat Grammar_Floor:660-677 + the live-seat rule) — the only components with model egress, rights explicitly ruled, not rowed | SURFACE_CONTRACTS kind=seat, rights as a column | OPEN — tier 2 |
| CI-B10 | blessed_subjects.json + Function_Voicings (Grammar_Floor:526-544, :613-626, :837-879) — stored data with writers, status ladders, and censuses that inv.verbatim depends on (registry drift silently breaks recompute); no dc rows | dc.blessed_subject (+ function-voicing rows) in Contract_Logic_Layer | OPEN — tier 2 |
| CI-B11 | the GATE-SUBJ battery (Grammar_Floor:546-572) is inv.no_invented_text's only mechanical enforcement for blessed words; the INVARIANTS enforced-by cell doesn't cite it | Technical INVARIANTS enforced-by + a Logic ds row | OPEN — tier 2 |
| CI-B12 | Law 9 / THE COVERAGE LINE incl. ACCESS-SCOPED searchability (personal trees owner-only — a data rule on the index; Design_Chatbot:672-714) absent from sc.ask_console promises | Surfaces GOVERNS + an ask_console promise | OPEN — tier 3 |
| CI-B13 | the GOVERNANCE JOURNAL (Design_Chatbot:790-809 — append-only, supersede-never-delete, replayed at boot, user trees as views) — stored data with a named writer, no dc row | Contract_Governance_Layer | OPEN — tier 3 |
| CI-B14 | L3 PROJECTIONS (Design_Graph_Engine:215-222 + Design_Chatbot:970-976) — the vector column is a field on EVERY node row, absent from both CONTRACT_FIELDS | ds.l3_projection + field rows both layers | OPEN — tier 3 |
| CI-B15 | Law 8 D1/D2/D3 (embedding model pinned as registry data, cache keyed (question, model), degradation ladder as data) governs console determinism, un-rowed | Surfaces GOVERNS | OPEN — tier 3 |
| CI-B16 | Law 1 THE GRAPH IS THE REALITY + the elimination principle (no pre-made mapping tables; only physics stays authored) — the standing ban the ask-index design sits inside, un-rowed | Surfaces GOVERNS, one row | OPEN — tier 3 |
| CI-B17 | the carried-laws paragraph (Design_Graph_Engine:300-311) holds four inv-grade laws with no inv row: conservation (cited by name in half a dozen contract cells with no id) · the literal law · the verification law · capacity discipline | Technical INVARIANTS: inv.conservation · inv.literal_law · inv.verification · inv.capacity | OPEN — tier 3 |

## C — CONSISTENCY: index, status, stale text

| id | finding | kind | proposed disposition | status |
|---|---|---|---|---|
| CI-C1 | live stale law: the COVERAGE INVARIANTS paragraph (Design_Graph_Engine:185-192) still speaks TWO invariants resting on the retired reads edge, present tense, never amended when era 3 collapsed them to ONE (side-targets == read-set, :363-366) | stale live text | amend the paragraph to the one-invariant form with an era-3 supersession note | LANDED 2026-09-17 |
| CI-C2 | INDEX drift, four cases: Grammar_Floor stamped v2.9.0 (file is v2.10.0, R12 ratified 09-16) · the a2 contingency presented live (probe GREEN 09-15, a1 CONFIRMED, a2 DEAD — Design_Graph_Engine:388-390) · Contract_Logic_Layer described as "5 findings FL1–FL5" (file carries FL1–FL8) · L1_KG1_CONTRACT_DATALOAD stamped RATIFIED (file header says DRAFT v0.1; §3c is one ratified section, not the doc) | index drift | four INDEX line corrections | LANDED 2026-09-17 |
| CI-C3 | NINE docs carry no Status line in the header (both Design_ docs, Grammar_Floor, Manifest_Build, Ruling_Center/Twin, four Audit_ files) — INDEX is the ONLY place their status exists, so nothing can contradict it; the C2 drifts are the visible portion | unverifiable status | add the one Status header line to each (their current INDEX stamps, corrected per C2, become the content); future sweeps turn mechanical | LANDED 2026-09-17 (ten headers; Audit_Literal_Law_Plan's "step F" stamp flagged unverifiable — needs Sunny) |
| CI-C4 | four CLOSED briefs exist with no INDEX record beyond the generic briefs/ row; and Contract_Technical's header names two layer docs that do not exist (Consumption, Governance) as "to follow" | index completeness | Sunny's call: per-brief INDEX lines vs one briefs/ row that carries a count; the header's "to follow" resolves when CI-B5 lands the docs | RULED (a) (Sunny "a", 2026-09-17): the folder line carries count + freshness, updated same breath as brief status moves; per-brief lines never — the naming law's intent at the briefs' grain. LANDED same breath; the "to follow" half stays with CI-B5 |
| CI-C5 | clean sweeps, recorded: clauseProvenance (retirement-marked only) · conflict (zero hits) · derivable_never_authored (FL6 bookkeeping only) · registry versions (1.45.0 current everywhere; lower versions all historical) · no dangling INDEX pointers · all existing Status words from the known set | clean | none — the row is the record | CLOSED |
